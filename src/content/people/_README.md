# people

One Markdown file per record; the file name is the URL. Files starting with an
underscore (like this one) are ignored by the site. Every record needs a `groups:`
list naming the CISD groups it belongs to. See MAINTENANCE.md for all the fields.
